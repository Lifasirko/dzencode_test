version: '3.8'

services:
  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: dzencode_db
      POSTGRES_USER: amol
      POSTGRES_PASSWORD: mypassword # TODO: винести в .env
    ports:
      - "5432:5432"

volumes:
  postgres_data:
